/**
 * test
 */
/**
 * Code that is used in other parts of this project/module. It serves very general(and to be honest, seemingly arbitrary) functions by
 * itself, but when built on by other packages, is actually useful.
 * <p>In the analogy, this api package only knows about itself and some jdk tools.
 * <p>This will also be used in similar projects, probably as a jar.
 * @author SId
 *
 */
package lcts.api;